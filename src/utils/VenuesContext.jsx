import { createContext, useContext, useState, useEffect } from 'react'
import { allVenues } from '../constants/apiEndpoints'
import { useAuth } from './AuthProvider'

const VenuesContext = () => {
  return <div>VenuesContext</div>
}

export default VenuesContext
